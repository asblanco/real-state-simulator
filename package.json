{
  "name": "real-state-simulator",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build src/index.ts --outdir dist",
    "test": "bun test",
    "dev": "bun build src/index.ts --outdir dist --watch & sleep 1 && bunx browser-sync start --server --files 'dist/**/*, index.html, styles.css' --no-notify --no-open"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "browser-sync": "^3.0.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "@tailwindcss/postcss": "^4.3.0",
    "chart.js": "^4.5.1",
    "daisyui": "5",
    "playwright": "^1.60.0",
    "tailwindcss": "^4.3.0"
  }
}
